note that an interface can have methods created in them without the body.
they are used as a base for other classes.
the body of the method in the method must be implemented in a class and then called then can be filled with code in the body